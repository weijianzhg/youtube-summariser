# LLM Configuration
# Supported providers: "openai" or "anthropic"
provider: "anthropic"

# Model settings per provider
openai:
  model: "gpt-4o"
  max_tokens: 3000

anthropic:
  model: "claude-sonnet-4-20250514"
  max_tokens: 3000

